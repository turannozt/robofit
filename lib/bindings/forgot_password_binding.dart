import 'package:get/get.dart';

// GetX Yapısı: Şifre Sıfırlama Sayfası Bağlama Sınıfı
class ForgotPasswordBinding implements Bindings {
  @override
  void dependencies() {
    // Bu sınıf, şifre sıfırlama sayfasının bağımlılıklarını yönetir.
    // Şu an için herhangi bir bağımlılık eklenmemiştir.
  }
}
